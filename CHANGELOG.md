# 变更记录

## 0.9.17 - 待发布

- 设置页新增配置导入/导出，备份覆盖设置、连接凭据和绑定链并标注应用及配置格式版本；运行日志不进入备份。
- 导入前校验完整关联并释放已接管限速，数据替换使用单事务；运行时速度、状态和接管快照不会跨设备恢复。

- 更新桌面入口图标资源版本，避免 fnOS 继续使用旧图标缓存。

- 使用 fnOS 官方 `@trimjs/web-app` SDK 获取平台主题，并监听 `os/theme` 宿主事件。
- 保留旧系统和独立浏览器访问时的主题降级兼容。

- 设置新增退出/解绑限速策略：恢复原值、解除限速、恢复绑定链最高限速。
- 持久化最高限速快照，支持删除绑定和重启后的释放；失败保留状态重试。
- 统一下载器测试与限速入口，使用具名注册类型；未知类型不再误走 qBittorrent。
- 分离数据库连接、迁移、设置、释放快照职责，提取设置 UI、HTTP 客户端、表单和图标。
- 删除确认无引用的旧日志格式函数、重复清理方法和无效样式。
- 修复释放后重新接管复用旧限速缓存，以及删除停用链可能释放其他启用链的问题。
- 新增释放、注册扩展、设置 API 和 UI 测试，并接入 CI。
- API 配置修改、轮询写入与释放统一命令协调；停机取消并排空命令，更换连接前使用旧连接释放。
- SQLite 使用单连接、WAL、立即事务和 busy_timeout；关闭嵌套游标，事务内复查链冲突并合并事件。
- 删除旧 bindings API/CRUD、兼容门面及未使用的 fnOS 本机 qB 探测；旧数据原子转换成功后清理旧表和规则字段，失败可重试。
- 增加并发删链、改规则、换连接、停机、迁移回滚和 SQLite 并发读写回归。
- 下载器速度标签简化为“当前/最大”，本地播放开关简化为“统计本地”。绑定链上下行规则使用独立小卡片，关闭限制方向时上下间距对称。
- 新日志统一使用 kb/s；旧日志在界面显示时转换 bytes/s、KB/s，不改写历史文件。亚千字节值保留小数。

## 0.9.11 - 2026-09-05

- 合并一次限速操作产生的重复日志，成功或失败仅保留一条完整结果。
- 目标限速没有变化时跳过下载器写入，不再产生无意义日志。
- 相同事件写入时更新已有记录，并在读取历史记录时折叠旧重复项。

## 0.9.10 - 2026-09-04

- 增加 SIGTERM/SIGINT 优雅关闭、HTTP 超时与轮询引擎协程收尾。
- 停止、升级或卸载时按单下载器独立超时执行 best-effort 原限速恢复，并写入文件日志。
- 调整接管顺序为先持久化原限速、再写入下载器，失败重试不会覆盖首次原值。
- 原值与当前限速相同时按 no-op 完成恢复，避免不必要的外部写入。
- 每次实际限速设置均写入文件日志和事件日志，并记录目标值及执行结果。
- 删除绑定链时立即恢复原限速；下载器停用后若恢复失败，后台仍会持续重试待恢复状态。
- 增加崩溃重启、恢复超时隔离和中间态回归测试，并在 CI 中启用格式检查与竞态检测。

## 0.9.9 - 2026-09-04

- 将应用内部按领域、影视库、下载器、绑定链、存储、服务与 API 分包。
- 将影视库和下载器抽象接口与具体实现拆分，便于后续增加 Provider。
- 将飞牛下载认证、网关、协议帧、加密、响应解析和探测逻辑按单一职责拆分。
- 将旧绑定关系迁移逻辑从绑定链持久化中独立。
- 整理 GitHub 发布目录、文档、源码归档和敏感信息检查流程。

## 0.7.8 - 2026-09-03

- 将影视库与下载器实现抽象为可扩展适配器，并引入多对多绑定链。
- 支持飞牛影视、Emby、Jellyfin 与通用 HTTP 人数接口。
- 支持飞牛下载、qBittorrent 与 Transmission。
- 上传、下载限制可按绑定链分别启用，并在停用、解绑或删除时恢复原值。
- 飞牛影视支持轮询、活跃窗口和本地播放计入策略。
- 增加日志分页、Debug 模式、主题跟随与 fnOS 风格管理界面。
- 完成飞牛下载原生认证、用户 ID 回退和限速读写流程。
- 完成桌面图标、iframe 启动、安全响应头和空白页兜底处理。
- 将仓库收敛为 `app/`、`scripts/`、`db/`、`packaging/` 与 `docs/`，分离源码、运行数据和发布资源。
- 将应用内部拆分为领域、影视库、下载器、绑定链、存储、服务与 API 包，并将飞牛认证、协议帧、加密和探测逻辑按单一职责分文件维护。

完整发布包请从 GitHub Releases 下载；仓库不提交构建产物和运行数据库。
