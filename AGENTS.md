# 项目协作约定

- 默认开发分支为 `dev`。首次发布后，所有新代码提交到 `dev`，不得直接修改/推送 `main`。
- 只有用户明确确认本次晋级后，才能创建 `dev` → `main` 的 PR；不得自动合并 PR。发布标签只能指向已进入 `main` 的提交，且需用户授权发布。
- 不提交真实数据库、密码、令牌、运行日志、测试机地址、构建包或工作工具目录。不要删除用户运行数据。
- 配置写入和设备限速操作统一通过 `service.Engine.Coordinate`。协调上下文不得传入异步 goroutine；适配器须响应 context 取消。
- 接口和具体实现同功能目录、分文件维护。删除代码前查明运行/测试/UI 引用；旧数据仅通过可重试迁移清理。
- 提交前执行 `gofmt`、`go test -race ./...`、`go vet ./...`、前端测试/构建，并同步文档；行为修改必须有回归测试。
